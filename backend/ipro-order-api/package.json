{
  "name": "ipro-order-api",
  "version": "2.0.0",
  "description": "iPro internal Shopify order lookup API for TeleSyriana dashboard using Shopify Dev Dashboard client credentials",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/shopify-token.js && node --check routes/orders.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
